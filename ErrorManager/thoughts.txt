Catch exceptions around queries

Every one minute, load new notifications

connect bulk_file to errors


//
Add action type to errors when resolving

Find a way of telling a user the file they uploaded is wrong
